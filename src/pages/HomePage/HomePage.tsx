import { Box } from "@chakra-ui/react";
import { Header, OpeningTime } from "../../components";

export const HomePage = () => {
  return (
    <Box>
      <Header />
      <OpeningTime />
    </Box>
  );
};
